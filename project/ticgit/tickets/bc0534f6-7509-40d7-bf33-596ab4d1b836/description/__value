Beads encourages commit messages to reference issue IDs so doctor checks can find work that landed while its issue stayed open. TicGit should scan recent git commits for TicGit IDs or configured patterns and report tickets that appear implemented but are still open, plus closed tickets with follow-up commits. This could start as a `ti doctor` check and later support `--fix` suggestions.